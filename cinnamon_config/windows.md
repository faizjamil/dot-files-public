# Windows

* Titlebar
  * Buttons
    * Button layout: `Right`
  * Actions
    * Action on title bar double-click: `Toggle Maximize`
    * Action on title bar middle-click: `None`
    * Action on title bar right-click: `Menu`
* Behavior
  * WIndow Focus
    * Window focus mode: `Click`
    * Give focus to new windows launched from a terminal: `on`
  * Moving and Resizing Windows
    * Location of newly opened windows: `Automatic`
    * Special key to move and rezie windows: `<Alt>`
  * Everything else disabled
* Alt-Tab
  * Alt-Tab
    * Alt-Tab switcher style: `Icons and thumbnails`
    * Move minimized windows to the end of the alt-tab switcher
    * Delay before displaying the alt-tab switcher (milliseconds): `100`
  * Everything else disabled
* Tiling
  * Enable all
