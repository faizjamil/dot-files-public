# Cinnamon Configuration

This folder contains various settings to configure cinnamon, each section is contained in it's own markdown file.

In general if a setting is not listed then it is disabled if it's a toggle or left to default if it's a dropdown

In menu applett settings:
Panel Height: 50
